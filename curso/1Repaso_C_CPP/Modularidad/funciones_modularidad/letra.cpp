/*
Repaso de funciones usando modularidad
Implementar en C/C++ soluciones a los siguientes requerimientos:

1. A ciertos estudiantes se les dice que su calificación final será el promedio de las
cuatro calificaciones más altas de entre las cinco que hayan obtenido en el curso.
Escribir un procedimiento llamado promclase con cinco parámetros de entrada (las
calificaciones obtenidas) y un parámetro de salida (la calificación promedio) que lleve
adelante el cálculo.

2. Determinar las raíces de la ecuación cuadrática ax2 + bx + c = 0, donde a, b y c son
números reales. Presentar la salida según se tenga: 1 raíz doble, 2 raíces reales, 2
raíces imaginarias.
3. Determinar la cantidad de números primos entre A y B (naturales positivos).
4. Determinar la cantidad de números primos gemelos entre A y B (naturales
positivos). Los primos gemelos son parejas de números primos con una diferencia
entre sí de 2, por ejemplo 3 y 5, 11 y 13.
5. Escribir una función alfa que tenga como parámetros un arreglo de caracteres
llamado frase (con un máximo de 100 caracteres), un natural llamado largo (número
de caracteres en la frase) y una variable de carácter llamada letra. La función alfa
indica el número de apariciones del carácter letra en el arreglo frase.
6. Escribir un procedimiento de clasificación por selección que ordene un arreglo de
enteros en orden descendente mediante la localización del valor más pequeño en
cada iteración y el intercambio de éste con el componente apropiado.
7. Escribir un procedimiento llamado cambio que tenga los parámetros matriz (arreglo
bidimensional de diez filas y diez columnas) y 2 variables naturales m y n. El
procedimiento cambio intercambia las filas m y n de matriz.
*/